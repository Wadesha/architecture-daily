# 2026-09-11

## architecture-daily

### 1. 省卷体 20 篇全部重写（质量收口）
昨天子代理写的 20 篇被切成 19–21 个百字段落、结尾注水，本轮全部推倒重写。
规范（写进 prompt 才有效）：**7 段 × 每段 250–350 汉字**，段段递进
（地理价值，沿革，总体形制，构造做法，塑像/装饰，体系地位，保存与价值），
禁用"综上所述/总之/值得注意的是/可见/不难看出"，禁止结尾连写数段感慨。
分批：晋 5、徽赣 5、闽粤 5 交给子代理；西南西北 5 撞 429（09:26 重置）后由主代理自己写完。

### 2. emoji 铁律（用户明确要求：禁止任何 emoji）
全站扫描发现 **159/200 个 HTML 混进 emoji**（， 等），
来源是各文章顶部/底部注入的 `<!-- ARCHIVE TOP/BOTTOM NAV -->` 导航条（还带 onclick）。
- 新增 `_scripts/sanitize_html.py`：删 emoji（保留 与正文，）、删导航块内 onclick、Top/Bottom 改纯文字。
- 清洗 154 个文件；复验 **emoji 残留 0**。
- 注意：index.html 自身 UI 的 onclick 保留；03_overseas 下 4 个采集页（docin_xibuyg / ja1915 / ja_date / ja_date1915）的 onclick 是原页面自带，暂留。

### 3. 关于"要不要整合成一个 HTML"（用户提问）
**结论：不要整合，保持多文件。** 实测数据：
- 194 篇总计 42.0 MB，最大单篇 2.1 MB， 单文件根本打不开。
- 只内联 ≤100 KB 的：130 篇 / 3.3 MB，仍有 64 篇大部头要外链。
- 多文件 = 永久 URL、增量更新、按需加载，index.json 就是靠 URL 索引的。
折中产物：`_scripts/build_archive_all.py`， `archive-all.html`（离线合辑，130 篇内联 + 64 篇链接，3.3 MB，已从 index 排除）。
脚本默认阈值 `ARCHIVE_MAX_KB=100`，可改。

### 3.5 篇幅补写（本轮后半）
审计发现 13 篇字数不足（主代理自己写的那批只有 950–1400 字，子代理写的都有 2000+）。
逐个插段补足，全部重新达到 1800–2100 字：
- 省卷体 6 篇（增冲/崇圣寺/张掖/莫高窟/苏公塔/武当山）：各补 2–3 段（聚落格局、使用方式、工艺细节、同类比较）
- 导读体 5 篇：各补 3 段（田野技术细节、人物制度传承、生平与成书、接受史、方法论）
- 鲁晓敏体 2 篇（党家村/喀什老城）：各补 2–3 段（院落礼制、缺水与营造、日常与人物、巷道砖语）
**坑**：Edit 插入新段时开头的 `<p>` 会被吃掉，导致段落合并。检测正则 `</p>\s*\n\s*\n([^<\s])`，
已写进一次性修复脚本；以后每次手工插段后都要跑一遍这个检测。

### 4. 环境坑
- **index.json 被另一个会话改写过**：同一份文件一会儿 194 篇一会儿 173 篇（build 脚本读到 173）。
 本站疑似有并发会话在改，写入关键文件后要复查。
- 369 个 OneDrive 占位文件仍不可读（"云文件提供程序未运行"），需用户启动 OneDrive。

## 维护脚本清单（_scripts/，已 gitignore）
`gen_index.js` / `regen_index.py`（重算台账，排除 index.html 与 archive-all.html）
`merge_paras.py`（合并过短段落，注意会吃掉 h2，需事后补回）
`sanitize_html.py`（去 emoji + 去导航 onclick）
`build_archive_all.py`（生成离线合辑）
`templates/t01~t05.html`（五套体风模板）
